[project]
name = "rag-manual-searcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "begsta", email = "s.begdash@gmail.com" }]
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.19.2",
    "asyncpg>=0.31.0",
    "bcrypt>=5.0.0",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "loguru>=0.7.3",
    "pydantic-settings>=2.15.0",
    "pyjwt>=2.13.0",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.4",
]

[tool.ruff]
line-length = 140
exclude = ["alembic"]

[dependency-groups]
dev = ["pre-commit>=4.6.2", "ruff>=0.16.6"]
